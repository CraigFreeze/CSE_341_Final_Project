const express = require('express');
const routes = express.Router();

const teacherController = require('../controllers/teacherController.js')

// routes.get('/', teacherController.getSingle(''))

module.exports = routes;