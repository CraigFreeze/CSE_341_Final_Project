const express = require('express');
const routes = express.Router();

const classController = require('../controllers/classController.js')

// routes.get('/', classController.getSingle(''))

module.exports = routes;